### Thesis Title: 
# Geospatial Tagging of Volunteered Place Descriptions: Bayesian Elastic Net and Extended FP-Growth Approaches

### Visit this link for complete GUI of the documentation of my thesis and the progress.
## https://osm.texodus.tech/(site is down because of AWS server destroyed in bahrain)



## Technical Overview

### 1. Data Collection (Extended Pipeline)
The project processes Volunteered Geographic Information (VGI) from **12 Pakistani cities**, including Islamabad, Lahore, Karachi, Rawalpindi, Kohat, Peshawar, Attock, Chitral, Skardu, Quetta, Sialkot, and Muzaffarabad. Data is collected using the Overpass API, tiled for efficiency, and preprocessed to handle Roman Urdu text (e.g., normalizing "chowk" to "intersection").

### 2. Hybrid Classification Model
We employ a **Hybrid Neural Network** that fuses two distinct feature sets:
- **Semantic Features**: Dense vector embeddings generated by **DistilBERT** (contextual understanding).
- **Linguistic Features**: A binary feature vector of high-utility Adjective-Noun pairs selected via **Bayesian Elastic Net (BEN)**.

These features are concatenated and passed through a Multi-Layer Perceptron (MLP) for final classification. This approach significantly outperforms standard Word2Vec baselines.

### 3. Bayesian Elastic Net (Feature Selection)
We use an approximated Bayesian Elastic Net to select the most relevant linguistic patterns from mined frequent itemsets. The objective function minimizes the regularized negative log-likelihood:

$$
L(w) = -\frac{1}{N} \sum_{i=1}^{N} [y_i \log(p_i) + (1 - y_i) \log(1 - p_i)] + \lambda \left[ \alpha ||w||_1 + \frac{1 - \alpha}{2} ||w||_2^2 \right]
$$

Where:
- $\lambda$ controls the overall regularization strength.
- $\alpha$ balances the **L1 (Lasso)** penalty (sparsity/Laplace prior) and the **L2 (Ridge)** penalty (grouping/Gaussian prior).

For any assistance you can always get help from here at faizan@texodus.tech

